function backdrop(props){
    return <div className='backdrop' onClick={props.onClickResponse}/>;
    // props.onClickResponse, executes the function Defined 'disableModalandBackdrop' in Todo.js
}

export default backdrop;